---
   - name: test update
     hosts: all
     become: yes
     tasks:
       - name: apt update
         shell: apt update
         when: ansible_os_family == "Debian"  
       - name: apt upgrade
         shell: apt upgrade -y
         when: ansible_os_family == "Debian"
       - name: yum update
         shell: yum update --y
         when: ansiblme_os_family == "CentOS"
